<%= form_for @campaign, {:class => 'form-horizontal'}  do |f| %>
    <%= input_text_form f, :name, Campaign.human_attribute_name(:name) %>
    <%= input_text_field_form f, :description, Campaign.human_attribute_name(:description) %>
    <% status_array = [['En cours', :in_progress], ['Interrompu', :interrupted], ['Terminé', :finished]] %>
    <%= input_select_form(f , :status, options_for_select(status_array, @campaign.status), Campaign.human_attribute_name(:status), {size: 1}) %>
    <%= input_select_form f , :game_master_id, options_from_collection_for_select(@users, :id, :name, @game_master_id), Campaign.human_attribute_name(:game_master_id), {size: 1} %>
    <%= submit_text_form f %>
<% end %>
