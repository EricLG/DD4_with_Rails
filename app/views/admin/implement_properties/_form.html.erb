<%= form_for([:admin, @implement_property]) do |f| %>
  <% if @implement_property.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@implement_property.errors.count, "error") %> prohibited this implement_property from being saved:</h2>

      <ul>
      <% @implement_property.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <p><%= f.text_field :name %></p>
    <p><%= f.text_area :description %></p>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
